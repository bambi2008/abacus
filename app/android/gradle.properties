org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Works around a Windows-specific Kotlin Build Tools API bug ("Could not
# close incremental caches") that fails compileDebugKotlin on plugin
# modules (posthog_flutter, image_picker_android, in_app_purchase_android)
# — a Windows file-handle release issue in the incremental compiler, not a
# code problem. See https://youtrack.jetbrains.com/issue/KT-61873.
kotlin.incremental=false
